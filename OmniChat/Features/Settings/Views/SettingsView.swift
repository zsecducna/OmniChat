//
//  SettingsView.swift
//  OmniChat
//
//  Root settings screen with navigation to all settings sections.
//

import SwiftUI

/// Root settings view with sections for providers, defaults, personas, etc.
struct SettingsView: View {
    var body: some View {
        Text("Settings View")
    }
}

#Preview {
    SettingsView()
}
