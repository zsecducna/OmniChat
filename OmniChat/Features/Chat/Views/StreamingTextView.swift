//
//  StreamingTextView.swift
//  OmniChat
//
//  Token-by-token rendering for streaming responses.
//

import SwiftUI

/// Renders streaming text as it arrives from the AI provider.
struct StreamingTextView: View {
    var body: some View {
        Text("Streaming Text View")
    }
}

#Preview {
    StreamingTextView()
}
